# WeChatRobot 适配微信 3.9 版本说明

## 已完成的工作

### 1. 版本检查更新
- ✅ 更新了 `InitWeChat.cpp` 中的版本检查函数，支持多个 3.9 版本：
  - 3.9.0.28
  - 3.9.2.23
  - 3.9.2.26
  - 3.9.5.81

### 2. 偏移地址更新
- ✅ 更新了 `offset.h` 中的关键偏移地址（基于 wxhelper 项目的 3.9.5.81 版本）：
  - **接收消息**: `WxReciveMessage = 0x1010d80` (kDoAddMsg)
  - **发送文本消息**: `WxSendMessage = 0xfcd8d0` (kSendTextMsg)
  - **发送图片消息**: `WxSendImageMsg = 0xfc3d30` (kSendImageMsg)
  - **发送文件消息**: `WxSendFileMsg = 0xdd27f0` (kSendFileMsg)
  - **好友列表**: `WxFriendList = 0xeab270` (kGetContactList)
  - **账户服务管理器**: `WxGetAccountServiceMgr = 0x8c1230` (kGetAccountServiceMgr)
  - **登录标志位**: `LoginSign_Offset = 0x7F8` (在账户服务中)

## 需要手动完成的工作

### 1. 更新登录检查逻辑
由于文件编码问题，需要手动更新 `InitWeChat.cpp` 中的 `CheckIsLogin()` 函数：

```cpp
// 获取账户服务管理器 (3.9版本)
typedef UINT64(*__GetAccountService)();
UINT64 GetAccountService()
{
	DWORD dwWeChatWinAddr = (DWORD)GetModuleHandle(L"WeChatWin.dll");
	UINT64 accout_service_addr = dwWeChatWinAddr + WxGetAccountServiceMgr;
	__GetAccountService GetSevice = (__GetAccountService)accout_service_addr;
	return GetSevice();
}

void CheckIsLogin()
{
	DWORD  dwWeChatWinAddr = (DWORD)GetModuleHandle(L"WeChatWin.dll");

	while (true)
	{
		// 3.9版本使用账户服务管理器检查登录状态
		UINT64 service_addr = GetAccountService();
		if (service_addr != 0)
		{
			UINT64 login_flag = *(UINT64*)(service_addr + LoginSign_Offset);
			if (login_flag != 0)
			{
				HWND hLogin = FindWindow(NULL, L"Login");
				if (hLogin == NULL)
				{
					OutputDebugStringA("未找到Login窗口");
					return;
				}
				COPYDATASTRUCT login_msg;
				login_msg.dwData = WM_Login;
				login_msg.lpData = NULL;
				login_msg.cbData = 0;
				SendMessage(hLogin, WM_COPYDATA, (WPARAM)hLogin, (LPARAM)&login_msg);
				break;
			}
		}

		Sleep(500);
	}
}
```

### 2. 更新 WndMsgLoop.cpp 中的登录检查
在 `InitWindow()` 函数中，需要更新登录检查逻辑：

```cpp
// 检测微信是否已登录 (3.9版本使用账户服务管理器)
UINT64 service_addr = GetAccountService();
UINT64 login_flag = 0;
if (service_addr != 0)
{
	login_flag = *(UINT64*)(service_addr + LoginSign_Offset);
}
if (login_flag == 0)	//等于0说明微信未登录
{
	// ... 后续代码
}
```

### 3. 更新消息接收和发送函数
需要根据 wxhelper 的实现方式，更新以下文件：
- `ChatRecord.cpp` - 消息接收处理
- `Function.cpp` - 消息发送函数
- `FriendList.cpp` - 好友列表获取

### 4. 更新个人信息获取
由于 3.9 版本不再使用固定的基址，需要通过账户服务管理器获取个人信息。参考 wxhelper 的 `GetSelfInfo()` 实现。

## 注意事项

1. **架构差异**: wxhelper 使用函数指针直接调用，而 WeChatRobot 使用 Hook 和汇编调用。部分偏移地址可能需要根据实际情况调整。

2. **数据结构和偏移**: 3.9 版本的数据结构可能已经改变，消息结构中的偏移地址（如 `MsgTypeOffset`, `MsgContentOffset` 等）可能需要重新分析。

3. **测试**: 适配完成后，需要在微信 3.9 版本上进行充分测试，确保所有功能正常工作。

4. **版本兼容性**: 当前适配主要基于 3.9.5.81 版本，其他 3.9.x 版本可能需要微调偏移地址。

## 参考资源

- wxhelper 项目: https://github.com/ttttupup/wxhelper
- wxhelper 3.9.5.81 偏移地址定义: `wxhelper/src/wechat_function.h`
- wxhelper 实现参考: `wxhelper/src/manager.cc`

## 下一步工作

1. 手动更新登录检查逻辑
2. 更新消息接收和发送函数
3. 更新好友列表获取函数
4. 更新个人信息获取函数
5. 测试所有功能
6. 根据测试结果调整偏移地址

